<div class="container-fluid">
  <div class="row">
    <% cache @products do %>
      <%= render @products %>
    <% end %>
  </div>
</div>
<br>
<% if current_user && current_user.admin? %>

<%= link_to 'New Product', new_product_path %>
<% end %>
